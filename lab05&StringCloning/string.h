#pragma once

//converts all chars in an array to upper case
void to_upper(char *s);

//counts vowels in a string
int vowel_count(const char *s);

//checks if an array is empty
bool empty(const char *s);

//prints what operating system was defined in compilation 
void print_os();
